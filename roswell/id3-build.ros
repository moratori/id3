#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
;  #+quicklisp(ql:quickload '(:id3) :silent t)
  )

(defpackage :ros.script.id3.3749094754
  (:use :cl))
(in-package :ros.script.id3.3749094754)


(defun main (&rest args)
  (print "sample app"))

;;; vim: set ft=lisp lisp:
